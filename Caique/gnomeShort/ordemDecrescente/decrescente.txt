# include <stdio.h>
# include <stdlib.h>
# include <ctype.h>
# include <string.h>
# include <stdbool.h>
# include <time.h>


int VectorSort[];
int size = 10;
int troca=0;
int comparacao=0;

void swap(int *A,int *B);
void GnomeSort(int VectorSort[],int size);
void complet(int vetor[], int tam);
void imprime(int *vetor, int tam);

int main (void){

clock_t Tempo[2];
Tempo[0] = clock();

complet(VectorSort, size);
//imprime(VectorSort, size);
GnomeSort(VectorSort,size);

    Tempo[1] = clock();
    float TempoGasto = (Tempo[1] - Tempo[0]) * 1000.0 / CLOCKS_PER_SEC;

    imprime(VectorSort,size);
    printf("Tempo gasto: %f ms.", TempoGasto);


    printf("\n trocas = %d", troca);
    printf("\n comparacao = %d", comparacao);
    return 0;
}

void complet(int vetor[], int tam){
    int k, aux=tam;
    for(k=0; k<tam; k++){
    VectorSort[k]=aux;
    aux--;
}

}

void swap(int *A, int *B){
    int C = *A;
    *A = *B;
    *B = C;
}

void imprime(int *vetor, int tam){
    int j;
    printf("\n");
for(j=0;j<tam;j++){
    printf("\n%d", VectorSort[j]);
}
    printf("\n");

}

void GnomeSort(int VectorSort[], int size){
 int next = 0, previous = 0;
 int i;

 for(i = 0; i < size ; i++) {

 //if(VectorSort[i + 1] == NULL){
        //break;
 if(VectorSort[i] > VectorSort[i + 1]) {
        comparacao++;
       // printf("AAAAAAAAAAAAAAAAA %d   %d      %d", VectorSort[i], VectorSort[i+1], VectorSort[110]);
  previous = i;
  next = i + 1;

  while(VectorSort[previous] > VectorSort[next])  {
 swap(&VectorSort[previous],&VectorSort[next]);
 troca++;
if(previous > 0)
   // printf("\nBBBBBBBBBBBB    %d", previous);
    previous--;
 if(next > 0)
 //printf("\nCCCCCCCCCCCC    %d", next);
    next--;
  }
 }//}
 }
}

